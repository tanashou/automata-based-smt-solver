Style and conventions (inferred)

- Python 3.11+ typing used across the codebase; type hints present in many modules.
- Naming: snake_case for functions and variables; PascalCase for classes.
- Tests use pytest; tests live under `tests/` mirroring `src/` structure.
- Linting uses Ruff with rules in `ruff.toml`.
- Pre-commit configured in repo to run checks pre-commit.
